import {AppRegistry} from 'react-native';
import AppWithNavigationState from './source/Navigation';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppWithNavigationState);
